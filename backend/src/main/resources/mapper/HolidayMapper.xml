<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ganttchart.mapper.HolidayMapper">

    <select id="selectByDateRange" resultType="com.ganttchart.entity.Holiday">
        SELECT * FROM holiday
        WHERE holiday_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY holiday_date ASC
    </select>

    <select id="selectDateStringsByRange" resultType="java.lang.String">
        SELECT DATE_FORMAT(holiday_date, '%Y-%m-%d') FROM holiday
        WHERE holiday_date BETWEEN #{startDate} AND #{endDate}
          AND holiday_type = 1
        ORDER BY holiday_date ASC
    </select>

</mapper>
